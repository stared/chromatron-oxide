[package]
name = "chromatron"
version = "0.1.0"
edition = "2024"
description = "Pixel-perfect recompilation of Chromatron (laser puzzle game)"

[dependencies]
sdl2 = { version = "0.37", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
